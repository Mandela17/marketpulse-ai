# ─── MarketPulse AI — Production Environment ───
# Non-secret production values. Secrets MUST be set in Vercel Dashboard (Production env).

# App URL (your Vercel production domain)
NEXT_PUBLIC_APP_URL=https://marketpulse-ai-26.vercel.app

# Upstox API — set in Vercel Dashboard → Settings → Environment Variables → Production
# NEXT_PUBLIC_UPSTOX_CLIENT_ID=  (set in Vercel Dashboard)
# UPSTOX_CLIENT_SECRET=          (set in Vercel Dashboard — server-side only)
