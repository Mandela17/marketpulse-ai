import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import Sidebar from "@/components/Sidebar";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "MarketPulse AI | Indian Stock Market Sentiment Intelligence",
  description: "AI-powered market sentiment analysis for Indian stocks. Track sector-wise sentiment, geopolitical impacts, and near-term market outlook for NSE/BSE stocks.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="min-h-full flex">
        <Sidebar />
        <main className="flex-1 min-h-screen" style={{ background: 'var(--bg-primary)', marginLeft: '16rem' }}>
          {children}
        </main>
      </body>
    </html>
  );
}
